import Section from "../Section";

export default function Hero({ id }) {
  return (
    <Section id={id}>
      <div className="text-center space-y-4">
        <h1 className="text-5xl font-bold">Rinesa Ahmedi</h1>
      </div>
    </Section>
  );
}
